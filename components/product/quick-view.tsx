"use client";import*as Dialog from"@radix-ui/react-dialog";import{AnimatePresence,motion,useReducedMotion}from"motion/react";import{Star,X}from"lucide-react";import{useState}from"react";import Link from"next/link";import type{Product}from"@/types/commerce";import{ProductMediaPlaceholder}from"./product-media-placeholder";import{ProductPrice}from"./product-price";import{ProductSwatches}from"./product-swatches";
export function QuickView({product,open,onOpenChange}:{product:Product;open:boolean;onOpenChange:(v:boolean)=>void}){const[selected,setSelected]=useState(product.variants[0].id),reduced=useReducedMotion(),variant=product.variants.find(v=>v.id===selected)??product.variants[0];return <Dialog.Root open={open} onOpenChange={onOpenChange}><AnimatePresence>{open&&<Dialog.Portal forceMount><Dialog.Overlay asChild><motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[70] bg-black/55"/></Dialog.Overlay><Dialog.Content asChild><motion.div initial={reduced?{opacity:0}:{opacity:0,y:24,scale:.985}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20}} transition={{duration:reduced?.01:.25}} className="fixed inset-x-0 bottom-0 z-[71] max-h-[94dvh] overflow-y-auto rounded-t-2xl bg-background p-4 pb-[max(1rem,env(safe-area-inset-bottom))] md:inset-auto md:left-1/2 md:top-1/2 md:w-[min(58rem,92vw)] md:-translate-x-1/2 md:-translate-y-1/2 md:rounded-none md:p-6"><Dialog.Close className="absolute right-3 top-3 z-10 grid size-11 place-items-center rounded-full bg-white" aria-label="Close quick view"><X/></Dialog.Close><div className="grid gap-6 md:grid-cols-[1.15fr_.85fr]"><ProductMediaPlaceholder product={product} color={variant.color}/><div className="flex flex-col justify-center"><p className="eyebrow text-muted-foreground">Quick view</p><Dialog.Title className="mt-2 font-display text-[clamp(2rem,5vw,3.5rem)] font-semibold leading-none tracking-[-.055em]">{product.name}</Dialog.Title><div className="mt-4"><ProductPrice product={product}/></div><p className="mt-4 flex items-center gap-2 text-sm"><Star size={15} fill="currentColor"/> {product.rating} <span className="text-muted-foreground">({product.reviewCount})</span></p><p className="mt-5 text-sm leading-6 text-muted-foreground">{product.description}</p><div className="mt-5"><p className="eyebrow mb-1">Color / {variant.color.name}</p><ProductSwatches variants={product.variants} selected={selected} onSelect={setSelected}/></div><div className="mt-4"><p className="eyebrow mb-2">Available sizes</p><div className="flex flex-wrap gap-2">{variant.sizes.map(s=><span className={`grid min-h-10 min-w-12 place-items-center border px-2 text-xs ${s.inStock?"border-black":"border-border text-muted-foreground line-through"}`} key={s.value}>{s.value}</span>)}</div></div><Link href={`/product/${product.slug}`} className="mt-7 flex min-h-12 items-center justify-center bg-black px-5 text-sm font-semibold text-white">VIEW PRODUCT</Link><button disabled className="mt-2 min-h-12 border border-black px-5 text-sm font-semibold disabled:cursor-not-allowed disabled:opacity-45">ADD TO BAG — DAY 3</button></div></div></motion.div></Dialog.Content></Dialog.Portal>}</AnimatePresence></Dialog.Root>}
