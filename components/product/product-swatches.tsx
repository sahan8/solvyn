"use client";import type{ProductVariant}from"@/types/commerce";export function ProductSwatches({variants,selected,onSelect,compact=false}:{variants:ProductVariant[];selected:string;onSelect:(id:string)=>void;compact?:boolean}){return <div className="flex flex-wrap gap-1" role="radiogroup" aria-label="Available colors">{variants.slice(0,compact?4:undefined).map(v=><button key={v.id} type="button" role="radio" aria-checked={selected===v.id} aria-label={v.color.name} onClick={e=>{e.preventDefault();e.stopPropagation();onSelect(v.id)}} className={`${compact?"size-8":"size-11"} grid place-items-center rounded-full`}><span className={`block size-4 rounded-full border border-black/20 ${selected===v.id?"ring-2 ring-black ring-offset-2":""}`} style={{background:v.color.hex}}/></button>)}</div>}
