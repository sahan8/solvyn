"use client";
import Image from "next/image";import {motion,useReducedMotion} from "motion/react";import {ButtonLink} from "@/components/ui/button";
export function Hero(){const reduced=useReducedMotion();return <section className="relative min-h-[clamp(43rem,100svh,68rem)] overflow-hidden bg-[#0a0a0a] text-white"><div className="container-shell relative flex min-h-[clamp(43rem,100svh,68rem)] flex-col justify-end pb-9 pt-28 md:pb-12"><motion.div initial={{opacity:0,y:reduced?0:16}} animate={{opacity:1,y:0}} transition={{duration:reduced?.1:.7}} className="relative z-10"><p className="eyebrow mb-4 text-[#c7ff2f]">Running / 01 — New season</p><h1 className="display"><span className="block">MOVE</span><span className="block">BEYOND.</span></h1></motion.div><motion.div initial={{opacity:0}} animate={{opacity:1}} transition={{delay:reduced?0:.35,duration:.45}} className="relative z-20 mt-8 flex flex-col gap-3 sm:flex-row"><ButtonLink href="#new-releases">Shop new releases</ButtonLink><ButtonLink href="/fit-check" variant="secondary">Find your fit</ButtonLink></motion.div><div className="pointer-events-none absolute inset-x-[2%] top-[16%] h-[48%] md:inset-x-[25%] md:top-[18%] md:h-[55%]"><Image src="/product-axis.svg" alt="SOLVYN Axis Trail campaign shoe" fill priority sizes="(max-width: 767px) 100vw, 70vw" className="object-contain drop-shadow-[0_30px_45px_rgba(0,0,0,.4)]"/></div><div className="absolute right-[var(--gutter)] top-28 hidden max-w-48 text-right text-sm text-white/60 md:block">Engineered traction. Responsive form. Built to cross the line and keep moving.</div></div></section>}
