"use client";
export const filterGroups = [{ key: "category", label: "Category", options: ["running", "lifestyle", "sneakers"].map(x => ({ value: x, label: x })) }, { key: "gender", label: "Gender", options: ["men", "women", "unisex"].map(x => ({ value: x, label: x })) }, { key: "size", label: "Size", options: [6, 7, 8, 9, 10, 11, 12].map(x => ({ value: String(x), label: `US ${x}` })) }, { key: "color", label: "Color", options: ["black", "white", "lime", "clay", "blue", "silver"].map(x => ({ value: x, label: x })) }, { key: "price", label: "Price", options: [{ value: "under-160", label: "Under $160" }, { value: "160-190", label: "$160–$190" }, { value: "over-190", label: "Over $190" }] }, { key: "collection", label: "Collection", options: ["velocity", "form", "arc", "studio"].map(x => ({ value: x, label: x })) }, { key: "availability", label: "Availability", options: [{ value: "in-stock", label: "In stock" }] }];
export function FilterPanel({ filters, onToggle }) { return <div className="divide-y divide-border">{filterGroups.map(g => <details key={g.key} className="group" open={g.key === "category" || g.key === "gender"}><summary className="flex min-h-12 cursor-pointer list-none items-center justify-between text-xs font-semibold uppercase tracking-wider">{g.label}<span aria-hidden>+</span></summary><div className="grid grid-cols-2 gap-1 pb-4">{g.options.map(o => { const active = filters[g.key]?.includes(o.value); return <button key={o.value} onClick={() => onToggle(g.key, o.value)} aria-pressed={active} className={`min-h-11 border px-3 text-left text-xs capitalize ${active ? "border-black bg-black text-white" : "border-border"}`}>{o.label}</button>; })}</div></details>)}</div>; }
