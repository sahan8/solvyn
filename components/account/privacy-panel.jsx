"use client";
import * as Dialog from "@radix-ui/react-dialog";
import { useState } from "react";
import { X } from "lucide-react";
export function PrivacyPanel(){const [open,setOpen]=useState(false);const [value,setValue]=useState("");return <div className="space-y-4"><Card title="MY DATA">Your profile is limited to account essentials. Data download is not available until persistent account storage is connected.</Card><Card title="SAVED FIT DATA">Device-saved Fit Profile data can be reviewed and deleted from My Fit. It is not silently synced.</Card><Card title="SAVED ADDRESSES">Saved addresses will be private account records with server-side ownership checks when persistence is connected.</Card><Card title="SESSIONS / DEVICES">Provider-backed session revocation is planned; no inactive control is presented as working.</Card><section className="border border-[#b42318]/50 bg-white p-6"><p className="eyebrow text-[#b42318]">Account deletion</p><h2 className="mt-3 font-display text-3xl font-semibold">DELETE YOUR ACCOUNT</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Deletion may remove your profile, addresses, wishlist, Fit Profile and preferences. Commercial records may need retention or anonymization under an applicable policy.</p><Dialog.Root open={open} onOpenChange={setOpen}><Dialog.Trigger asChild><button className="mt-5 min-h-12 border border-[#b42318] px-5 font-semibold text-[#b42318]">DELETE ACCOUNT</button></Dialog.Trigger><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-[70] bg-black/60"/><Dialog.Content className="fixed left-1/2 top-1/2 z-[71] w-[calc(100%-2rem)] max-w-lg -translate-x-1/2 -translate-y-1/2 bg-white p-6 shadow-2xl" aria-describedby="delete-description"><div className="flex items-start justify-between gap-5"><Dialog.Title className="font-display text-3xl font-semibold">DELETE THIS ACCOUNT?</Dialog.Title><Dialog.Close aria-label="Close delete account dialog" className="grid size-11 place-items-center"><X/></Dialog.Close></div><Dialog.Description id="delete-description" className="mt-4 text-sm leading-6 text-muted-foreground">This cannot be undone. Type DELETE to confirm. Production deletion requires recent authentication; deletion is unavailable in this build.</Dialog.Description><label className="mt-5 block text-sm font-medium" htmlFor="delete-confirmation">Confirmation<input id="delete-confirmation" value={value} onChange={(e)=>setValue(e.target.value)} autoComplete="off" className="mt-2 min-h-12 w-full border border-black/25 px-3"/></label><button disabled={value!=="DELETE"} className="mt-5 min-h-12 w-full bg-[#b42318] px-5 font-semibold text-white disabled:opacity-40" onClick={()=>{}}>DELETE ACCOUNT</button><p className="mt-3 text-xs text-muted-foreground">The operation remains unavailable until secure persistence and recent-authentication checks exist.</p></Dialog.Content></Dialog.Portal></Dialog.Root></section></div>}
function Card({title,children}){return <section className="border border-black/15 bg-white p-6"><h2 className="eyebrow">{title}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">{children}</p></section>}
