"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
const items=[['/account','Overview'],['/account/profile','Profile'],['/account/addresses','Addresses'],['/account/fit','My Fit'],['/account/wishlist','Wishlist'],['/account/orders','Orders'],['/account/privacy','Privacy']];
export function AccountNav(){const path=usePathname();return <><label className="block lg:hidden"><span className="eyebrow">Account section</span><select value={path} onChange={(e)=>location.assign(e.target.value)} className="mt-2 min-h-12 w-full border border-black/20 bg-white px-3">{items.map(([href,label])=><option key={href} value={href}>{label}</option>)}</select></label><nav aria-label="Account" className="hidden lg:block"><p className="eyebrow mb-5 text-muted-foreground">My account</p><ul className="space-y-1">{items.map(([href,label])=><li key={href}><Link aria-current={path===href?'page':undefined} className={`flex min-h-12 items-center border-l-2 px-4 text-sm font-semibold uppercase ${path===href?'border-black bg-white':'border-transparent hover:bg-black/5'}`} href={href}>{label}</Link></li>)}</ul><form action="/api/auth/signout" method="post"><button className="mt-6 min-h-12 px-4 text-sm font-semibold underline" type="submit">SIGN OUT</button></form></nav></>}
