import{notFound}from"next/navigation";import{Suspense}from"react";import{SiteHeader}from"@/components/navigation/site-header";import{SiteFooter}from"@/components/layout/site-footer";import{ShopExperience}from"@/components/shop/shop-experience";import{collectionMeta,collectionSlugs,productsForCollection,type CollectionSlug}from"@/lib/commerce/products";import{ProductGridSkeleton}from"@/components/feedback/skeletons";export function generateStaticParams(){return collectionSlugs.map(collection=>({collection}))}export async function generateMetadata({params}:{params:Promise<{collection:string}>}){const{collection}=await params;return{title:collectionMeta[collection as CollectionSlug]?.title??"Collection"}}export default async function CollectionPage({params}:{params:Promise<{collection:string}>}){const{collection}=await params;if(!collectionSlugs.includes(collection as CollectionSlug))notFound();const meta=collectionMeta[collection as CollectionSlug];return <><SiteHeader/><Suspense fallback={<ProductGridSkeleton/>}><ShopExperience products={productsForCollection(collection)} title={meta.title} description={meta.description}/></Suspense><SiteFooter/></>}
