import { PageFrame } from "@/components/account/page-frame";import { requireUser } from "@/lib/auth/session";import { listOrdersForUser } from "@/lib/account/orders";
export const metadata={title:"Orders"};export default async function Page(){const user=await requireUser('/account/orders');const orders=await listOrdersForUser(user);return <PageFrame eyebrow="Orders" title="ORDER HISTORY.">{orders.length?null:<div className="border border-black/15 bg-white p-7"><h2 className="font-display text-2xl font-semibold">NO ORDERS YET.</h2><p className="mt-3 text-sm text-muted-foreground">Your completed orders will appear here.</p></div>}</PageFrame>}
