"use client";import{useSyncExternalStore}from"react";const KEY="solvyn:wishlist";let ids=new Set<string>(),loaded=false;const listeners=new Set<()=>void>();function load(){if(loaded||typeof window==="undefined")return;loaded=true;try{const v=JSON.parse(localStorage.getItem(KEY)??"[]");if(Array.isArray(v))ids=new Set(v.filter(x=>typeof x==="string").slice(0,100));}catch{ids=new Set()}}function subscribe(fn:()=>void){load();listeners.add(fn);return()=>listeners.delete(fn)}function snapshot(){load();return[...ids].sort().join(",")}export function useWishlist(){const value=useSyncExternalStore(subscribe,snapshot,()=>"");const values=value?value.split(","):[];return{ids:values,has:(id:string)=>values.includes(id),toggle:(id:string)=>{load();if(ids.has(id))ids.delete(id);else ids.add(id);try{localStorage.setItem(KEY,JSON.stringify([...ids]))}catch{}listeners.forEach(fn=>fn())}}}
